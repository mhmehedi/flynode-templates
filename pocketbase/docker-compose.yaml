services:
  pocketbase:
    image: 'ghcr.io/muchobien/pocketbase:latest'
    environment:
      {}
    expose:
      - '8090'
    volumes:
      - pocketbase-data:/pb_data

    restart: unless-stopped

volumes:
  pocketbase-data:
